# 📋 OPPO Live Photo Viewer - v1.4 文件列表优化

## ✅ 文件列表UI优化

### 🎯 改进点

**问题：** 文件列表中显示完整路径，文件名太长，不便阅读

**解决：**
- 添加文件夹路径显示在列表上方
- 文件列表只显示文件名
- 自动换行显示长路径

---

## 🎨 新UI布局

### 左侧文件面板

```
┌──────────────────────┐
│  文件列表           │  ← 标题
├──────────────────────┤
│ 当前文件夹:           │  ← 文件夹路径（自动换行）
│ /home/user/Pictures/ │
│ /oppo/              │
├──────────────────────┤
│ [打开文件] [打开文件夹]│  ← 按钮
├──────────────────────┤
│ IMG20260117...jpg   │  ← 文件列表（只显示文件名）
│ IMG20260118...jpg   │
│ ...                 │
└──────────────────────┘
```

---

## 🔧 技术实现

### 1. 添加文件夹路径标签

```python
# 文件夹路径标签
self.folder_path_label = QLabel("当前文件夹: 未选择")
self.folder_path_label.setStyleSheet(
    "font-size: 11px; color: #666666; padding: 5px;"
)
self.folder_path_label.setWordWrap(True)  # 自动换行
layout.addWidget(self.folder_path_label)
```

### 2. 文件名到路径映射

```python
# 在 MainWindow.__init__ 中添加
self.current_folder = None  # 当前文件夹路径
self.current_files = {}  # 文件名到完整路径的映射
```

### 3. 扫描文件夹优化

```python
def scan_folder(self, folder_path):
    """扫描文件夹中的图片"""
    self.current_folder = Path(folder_path)

    # 更新文件夹路径标签
    self.folder_path_label.setText(
        f"当前文件夹: {self.current_folder}"
    )

    # 查找文件
    for pattern in patterns:
        for file_path in sorted(folder.glob(pattern)):
            # 只显示文件名
            filename = file_path.name
            # 存储完整路径到映射
            self.current_files[filename] = str(file_path)
            self.file_list.addItem(filename)
```

### 4. 文件选择优化

```python
def on_file_selected(self, item):
    """文件被选中"""
    filename = item.text()
    # 从映射中获取完整路径
    if filename in self.current_files:
        file_path = self.current_files[filename]
        self.load_photo(file_path)
```

---

## 📊 对比表格

| 特性 | v1.3 | v1.4 |
|------|------|------|
| 文件列表显示 | 完整路径 | 只显示文件名 |
| 文件名长度 | 很长 | 简洁 |
| 文件夹路径 | 不显示 | 显示在顶部 |
| 长路径处理 | 横向滚动 | 自动换行 |
| 可读性 | 一般 | ✅ 更好 |

---

## 💡 使用体验

### 打开文件夹后

**v1.3:**
```
┌──────────────────────┐
│ /home/user/Pictures/│
│ /oppo/IMG202601...│  ← 完整路径，太长
│ /oppo/IMG202601...│
│ /oppo/IMG202601...│
└──────────────────────┘
```

**v1.4:**
```
┌──────────────────────┐
│ 当前文件夹:           │
│ /home/user/Pictures/ │  ← 路径清晰
│ /oppo/              │  ← 自动换行
├──────────────────────┤
│ IMG20260117...jpg   │  ← 只显示文件名
│ IMG20260118...jpg   │
│ IMG20260124...jpg   │
└──────────────────────┘
```

---

## 🎯 优势

1. **可读性提升** ✨
   - 文件名简洁，易于扫描
   - 长文件名不会被截断

2. **信息清晰** ✨
   - 文件夹路径独立显示
   - 清晰知道当前浏览位置

3. **空间利用** ✨
   - 长路径自动换行
   - 文件列表更紧凑

4. **用户体验** ✨
   - 快速识别文件名
   - 轻松找到目标文件

---

## 📦 新版本

**版本：** v1.4
**位置：** `/home/yezichao/.openclaw/workspace/oppo-live-viewer/dist/OPPO-Live-Viewer`
**大小：** 约76 MB

---

## 🚀 立即使用

**双击运行：**
```
/home/yezichao/.openclaw/workspace/oppo-live-viewer/dist/OPPO-Live-Viewer
```

**使用方法：**
1. 点击"打开文件夹"
2. 查看顶部显示的文件夹路径
3. 浏览简洁的文件名列表
4. 点击文件快速加载

---

## 📋 Git提交

```bash
78cf0fb Improve file list: show folder path and shorten filenames
52df42c Update documentation for v1.3
8202173 Simplify UI: remove redundant GroupBoxes and fix jitter
5a3c738 Update documentation for v1.2
b7e0d99 Add support for normal images and Live indicator
```

---

## 🔍 版本历史

| 版本 | 日期 | 主要改进 |
|------|------|----------|
| v1.0 | 2026-02-27 | 初始版本 |
| v1.1 | 2026-02-27 | 添加循环和静音控制 |
| v1.1.1 | 2026-02-27 | 修复闪退问题 |
| v1.2 | 2026-02-27 | 支持普通图片和Live指示器 |
| v1.3 | 2026-02-27 | UI优化：移除冗余，修复抖动 |
| v1.4 | 2026-02-27 | **文件列表优化：显示路径，简化文件名** ⭐ |

---

**更新时间：** 2026-02-27 17:35
**版本：** v1.4
**改进：** 文件列表UI优化
